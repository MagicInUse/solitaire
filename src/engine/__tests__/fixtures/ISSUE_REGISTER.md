# Solitaire Logic — Issue Register (generated)

_Auto-generated by `src/engine/__tests__/register.test.ts` from the AI simulation harness + oracle. Do not edit by hand — regenerate with `WRITE_REGISTER=1 pnpm test`._

**Scope:** 40 seeded deals (`sim-0`…`sim-39`), draw-1, unlimited recycle.

**Outcome distribution (first decisive event per seed):**

| Verdict | Seeds |
| --- | ---: |
| FALSE-DEAD-MODAL | 18 |
| AI-IDLE-NOT-DEAD | 3 |
| AI-LOOP | 11 |
| WON | 8 |

## Issues (severity-ranked)

### 1. [FALSE-DEAD-MODAL] Dead-game modal fires while legal moves remain

- **Axis:** AXIS 1 — Liveness
- **Severity:** 1 (1 = highest)
- **Affected seeds (18):** `sim-3`, `sim-5`, `sim-10`, `sim-12`, `sim-13`, `sim-14`, `sim-15`, `sim-16`, `sim-17`, `sim-18`, `sim-22`, `sim-23`, `sim-25`, `sim-27`, `sim-29`, `sim-32`, `sim-35`, `sim-36`

<details><summary>Representative root-cause drill</summary>

```
SEED sim-3 — FALSE-DEAD-MODAL at step 83
  board: F=6 hidden=7 emptyCols=0 stock=0 waste=6
  production isDeadGame() => DEAD
  oracle livenessOracle()  => ALIVE
  legal moves the oracle found (the modal ignored): 8
    • 10♠ : tableau0 → tableau col 4  —  tableau→tableau; isDirectProgress=false
    • 4♠ : tableau0 → tableau col 5  —  tableau→tableau; isDirectProgress=false
    • Q♥ : tableau1 → tableau col 2  —  tableau→tableau; isDirectProgress=false
    • 10♦ : tableau1 → tableau col 6  —  tableau→tableau; isDirectProgress=false
    • 3♣ : tableau3 → tableau col 1  —  tableau→tableau; isDirectProgress=false
    • A♦ : foundation0 → tableau col 0 [back-move]  —  back-move; isProductiveBackMove=false
    • 2♥ : foundation1 → tableau col 3 [back-move]  —  back-move; isProductiveBackMove=false
    • 3♠ : foundation2 → tableau col 1 [back-move]  —  back-move; isProductiveBackMove=false
  computeHints()=8 raw, filterUsefulHints()=0 useful
  diagnosis: every legal move is a back-move or shuffle that isDirectProgress/
             isProductiveBackMove rejects, so isDeadGame reports DEAD — but the
             ratified liveness rule counts ANY legal move as ALIVE.
```

</details>

### 2. [AI-IDLE-NOT-DEAD] AI gives up though dead-game detector reports the game alive

- **Axis:** AXIS 1↔3 — Drift
- **Severity:** 2 (1 = highest)
- **Affected seeds (3):** `sim-2`, `sim-9`, `sim-31`

<details><summary>Representative root-cause drill</summary>

```
SEED sim-2 — AI-IDLE-NOT-DEAD at step 97
  board: F=17 hidden=4 emptyCols=1 stock=0 waste=0
  production isDeadGame() => NOT DEAD, yet getAIMove() => idle (self-contradiction)
  computeHints()=10 raw, filterUsefulHints()=0 useful
  oracle legal moves: 10
    • K♥ : tableau0 → tableau col 4
    • J♥ : tableau0 → tableau col 3
    • 5♦ : tableau0 → tableau col 6
    • K♣ : tableau1 → tableau col 4
    • K♠ : tableau2 → tableau col 4
    • 10♥ : tableau2 → tableau col 1
    • 6♦ : tableau2 → tableau col 5
    • K♦ : tableau3 → tableau col 4
    • 4♣ : foundation0 → tableau col 0 [back-move]
    • 6♥ : foundation1 → tableau col 5 [back-move]
  diagnosis: filterUsefulHints (display copy) suppresses every move so the AI
             idles, while isDeadGame (detection copy) still sees progress — the
             two parallel progress definitions have drifted apart.
```

</details>

### 3. [AI-LOOP] AI livelocks, cycling without genuine progress

- **Axis:** AXIS 3 — Redundancy
- **Severity:** 3 (1 = highest)
- **Affected seeds (11):** `sim-0`, `sim-4`, `sim-7`, `sim-8`, `sim-19`, `sim-26`, `sim-30`, `sim-33`, `sim-34`, `sim-38`, `sim-39`

<details><summary>Representative root-cause drill</summary>

```
SEED sim-0 — AI-LOOP at step 2000
  board: F=14 hidden=6 emptyCols=1 stock=2 waste=3
  filterUsefulHints()=0 useful move(s):
  diagnosis: the AI keeps replaying moves that return to a prior state; each may
             pass the single-step redundancy check yet the sequence forms a cycle.
```

</details>

